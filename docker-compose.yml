services:
  copy-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: copy-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - HTTPS_PROXY=${SOCKS5_PROXY_URL:-}
    dns:
      - 8.8.8.8
      - 1.1.1.1
